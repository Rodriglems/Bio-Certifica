import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.agroecologia.app',
  appName: 'App Agroecologia',
  webDir: 'build'
};

export default config;
